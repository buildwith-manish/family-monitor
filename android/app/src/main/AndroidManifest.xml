<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Internet / Network -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE"/>

    <!-- Camera / Audio -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>

    <!-- Location -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION"/>

    <!-- Notifications (Android 13+) -->
    <uses-permission
        android:name="android.permission.POST_NOTIFICATIONS"
        android:minSdkVersion="33"/>

    <!-- Foreground Service (base) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>

    <!-- FIX-MANIFEST-01: FOREGROUND_SERVICE_MEDIA_PROJECTION is required on
         Android 14+ (API 34+) for any service that calls startForeground()
         with FOREGROUND_SERVICE_TYPE_MEDIA_PROJECTION. Without this permission
         the system throws ForegroundServiceStartNotAllowedException silently,
         the foreground state is never set, and the service is killed within 5s. -->
    <uses-permission
        android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION"
        android:minSdkVersion="34"/>

    <!-- FIX-MANIFEST-02: FOREGROUND_SERVICE_DATA_SYNC is required for the
         ScreenCaptureService's fallback startFgWithoutCapture() path, which
         uses DATA_SYNC type when no projection token is available. Without this
         the fallback also throws on API 34+. -->
    <uses-permission
        android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"
        android:minSdkVersion="34"/>

    <uses-permission
        android:name="android.permission.FOREGROUND_SERVICE_CAMERA"
        android:minSdkVersion="34"/>
    <uses-permission
        android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE"
        android:minSdkVersion="34"/>
    <uses-permission
        android:name="android.permission.FOREGROUND_SERVICE_LOCATION"
        android:minSdkVersion="29"/>

    <!-- Boot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <!-- FIX-MANIFEST-03: WAKE_LOCK is required for PowerManager.PARTIAL_WAKE_LOCK
         in ScreenCaptureService, WatchdogService, and WatchdogReceiver.
         The old manifest already had this but it is explicitly confirmed here. -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>

    <!-- Usage Stats -->
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions"/>

    <!-- Contacts / SMS -->
    <uses-permission android:name="android.permission.READ_CONTACTS"/>
    <uses-permission android:name="android.permission.READ_CALL_LOG"/>
    <uses-permission android:name="android.permission.READ_SMS"/>
    <uses-permission android:name="android.permission.RECEIVE_SMS"/>

    <!-- Storage -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29"/>
    <uses-permission
        android:name="android.permission.READ_MEDIA_IMAGES"
        android:minSdkVersion="33"/>
    <uses-permission
        android:name="android.permission.READ_MEDIA_VIDEO"
        android:minSdkVersion="33"/>

    <!-- Bluetooth -->
    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <uses-permission
        android:name="android.permission.BLUETOOTH_CONNECT"
        android:minSdkVersion="31"/>
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:minSdkVersion="31"/>

    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.PROCESS_OUTGOING_CALLS"/>

    <!-- FIX-MANIFEST-04: SCHEDULE_EXACT_ALARM retained as best-effort for watchdog.
         SecurityException is caught and falls back to inexact alarm. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"
        android:minSdkVersion="31"/>

    <uses-feature android:name="android.hardware.camera"       android:required="false"/>
    <uses-feature android:name="android.hardware.camera.front" android:required="false"/>

    <!-- FIX-MANIFEST-05: FOREGROUND_SERVICE_SPECIAL_USE is required for
         WatchdogService which uses foregroundServiceType="specialUse".
         This was present in the original manifest and is retained. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"/>

    <application
        android:name="${applicationName}"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:hardwareAccelerated="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- ── Main Activity ─────────────────────────────── -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"/>
        </activity>

        <!-- ── Flutter Foreground Task ───────────────────── -->
        <service
            android:name="com.pravera.flutter_foreground_task.service.ForegroundService"
            android:exported="false"
            android:foregroundServiceType="dataSync"/>

        <!-- ── Flutter Background Service ───────────────── -->
        <!-- camera|microphone|dataSync: background isolate owns WebRTC peer connection. -->
        <service
            android:name="id.flutter.flutter_background_service.BackgroundService"
            android:exported="false"
            tools:replace="android:exported,android:foregroundServiceType"
            android:foregroundServiceType="camera|microphone|dataSync"/>

        <!-- ── Screen Capture (MediaProjection) ─────────── -->
        <!-- FIX-MANIFEST-06: stopWithTask="false" ensures ScreenCaptureService
             continues running when the task (app) is swiped from recents.
             Without this, swiping the app kills the service on non-Samsung OEMs
             because the default is stopWithTask="true" for most services. -->
        <service
            android:name=".ScreenCaptureService"
            android:exported="false"
            android:stopWithTask="false"
            android:foregroundServiceType="mediaProjection|dataSync"/>

        <!-- ── Boot Receiver ─────────────────────────────── -->
        <!-- FIX-MANIFEST-07: Added LOCKED_BOOT_COMPLETED and MY_PACKAGE_REPLACED.
             directBootAware="false" is correct — we read credential-encrypted
             SharedPreferences; we skip prefs access on LOCKED_BOOT_COMPLETED. -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true"
            android:enabled="true"
            android:directBootAware="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
            </intent-filter>
        </receiver>

        <!-- ── Notification Listener ─────────────────────── -->
        <service
            android:name=".ScreenNotificationListener"
            android:exported="true"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService"/>
            </intent-filter>
        </service>

        <!-- ── MediaProjection Consent Activity ──────────── -->
        <activity
            android:name=".StealthActivity"
            android:exported="false"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:taskAffinity=""/>

        <!-- ── PIN Verification Gate ──────────────────────── -->
        <activity
            android:name=".PinVerifyActivity"
            android:exported="false"
            android:theme="@android:style/Theme.Light.NoTitleBar.Fullscreen"
            android:launchMode="singleTop"
            android:excludeFromRecents="true"
            android:taskAffinity="com.example.family_monitor.pin"/>

        <!-- ── Watchdog Alarm Receiver ───────────────────── -->
        <!-- FIX-MANIFEST-08: WatchdogReceiver must NOT be exported; it only
             receives internal alarms. The alarm PendingIntent uses FLAG_IMMUTABLE
             which prevents external apps from firing it. -->
        <receiver
            android:name=".WatchdogReceiver"
            android:exported="false"/>

        <!-- ── Accessibility Service ─────────────────────── -->
        <service
            android:name=".AppBlockAccessibilityService"
            android:exported="true"
            android:label="Family Monitor App Block"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService"/>
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config"/>
        </service>

        <!-- ── Secret Dialer Code (*#9527#) ─────────────── -->
        <receiver
            android:name=".DialerCodeReceiver"
            android:exported="true">
            <intent-filter android:priority="999">
                <action android:name="android.intent.action.NEW_OUTGOING_CALL"/>
            </intent-filter>
        </receiver>

        <!-- ── Device Administrator ──────────────────────── -->
        <receiver
            android:name=".FamilyDeviceAdminReceiver"
            android:exported="true"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin_policies"/>
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED"/>
            </intent-filter>
        </receiver>

        <!-- ── Flutter metadata ──────────────────────────── -->
        <meta-data android:name="flutterEmbedding" android:value="2"/>
        <meta-data android:name="io.flutter.embedding.android.EntryPoint"
            android:value="${dartEntrypoint}"/>

        <!-- ── Watchdog Service ──────────────────────────── -->
        <!-- FIX-MANIFEST-09: stopWithTask="false" keeps WatchdogService alive
             even when the user swipes the app from recents. This is the same
             hardening applied to ScreenCaptureService above. -->
        <service
            android:name=".WatchdogService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            android:stopWithTask="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Used for continuous parental monitoring of child devices"/>
        </service>

        <!-- ── Screen Stream Service (WebSocket Streaming) ── -->
        <!-- STREAM-01: Low-latency screen streaming service that captures frames
             via VirtualDisplay + ImageReader and pushes them over WebSocket to
             a relay server. Replaces the slow Firebase RTDB base64 relay. -->
        <service
            android:name=".ScreenStreamService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="mediaProjection|dataSync"
            android:stopWithTask="false"/>
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>

</manifest>
